% Define the function using inline
f = inline('1/(1+x^2)');

a=0;
b=1;
h1=1;
h2=0.1;

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
I_a=(h1 / 2) * (feval(f, a) + feval(f, b));
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
I_b=h1/3*(feval(f,b)+4*feval(f,a+h1)+feval(f,a));
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
I_d=trapz(a:h2:b,f(a:h2:b));
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
I_e=Simpson(f(a:h2:b),h2);
